
import React from "react";

function NoHeaderLayout({ children }) {
    return (
        <div className='App'>
            {children}
        </div>
    );
}

export default NoHeaderLayout;
